'''
将数组拆分成左右两个部分，直至形成单个元素，对单个元素排序，元素来自于哪个分组，就将它合并到哪个分组

最优时间复杂度，最差时间复杂度O（n*logn）

稳定

但存在额外的空间开销
'''





def solution(li):
    n = len(li)
    if n <= 1:
        return li
    else:
        leftli = solution(li[:n//2])
        rightli = solution(li[n//2:])
    left = right = 0
    res = []
    while left < len(leftli) and right <len(rightli):
        if li[left] <= li[right]:
            res.append(li(left))
            left += 1
        else:
            res.append(li[right])
            right += 1
    res += li[left::]
    res += li[right::]
    return res
